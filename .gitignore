# This directory is both the skill's template payload and a runnable app.
# Everything a build produces stays out of the plugin repo.
node_modules/
dist/
.astro/
.wrangler/
*.tsbuildinfo
.DS_Store

# Tile build scratch — archives are hundreds of MB and belong in R2, not git.
.tiles-build/
